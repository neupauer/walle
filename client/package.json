{
  "private": true,
  "scripts": {
    "start": "rimraf dist && parcel serve src/index.html --out-dir dist --port 3000 --open",
    "build": "rimraf dist && parcel build src/index.html --detailed-report --out-dir dist"
  },
  "dependencies": {
    "classnames": "^2.2.6",
    "lodash": "^4.17.15",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "rxjs": "^6.5.3",
    "socket.io-client": "^2.3.0",
    "tailwindcss": "^1.1.2"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^1.2.0",
    "autoprefixer": "^9.6.1",
    "parcel-bundler": "^1.12.3",
    "rimraf": "^3.0.0"
  }
}
